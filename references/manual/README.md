# references/manual

Coloque aqui o PDF de referencia:

```
references/manual/2019-manual-tecnico-curso-de-motores-web.pdf
```

Este PDF e qualquer material extraido dele sao **gitignored** e **nunca** entram no bundle de
producao, em `public/`, `dist/` ou em rota acessivel. Sao usados apenas como fonte interna de
estudo. Rode `npm run extract-manual` para gerar o indice de trabalho local em
`references/manual/extracted/` (tambem gitignored).

Este arquivo `README.md` e um manifesto textual seguro para versionamento (nao contem conteudo
protegido).
